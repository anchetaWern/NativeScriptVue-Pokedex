// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.wern.nsvpokedex"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
